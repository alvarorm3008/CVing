# Copy to Vercel → Project → Settings → Environment Variables
# Required when frontend is on Vercel and backend on Render (or elsewhere).
# Redeploy after adding.

VITE_API_URL=https://YOUR-SERVICE.onrender.com
